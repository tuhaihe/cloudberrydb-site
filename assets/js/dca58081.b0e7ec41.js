"use strict";(self.webpackChunkApache_Cloudberry_Incubating_website=self.webpackChunkApache_Cloudberry_Incubating_website||[]).push([[80829],{99492:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>a,default:()=>h,frontMatter:()=>i,metadata:()=>o,toc:()=>l});var s=t(85893),r=t(11151);const i={title:"Common Table Expressions"},a="WITH Queries (Common Table Expressions)",o={id:"operate-with-data/sql-queries/cte-queries",title:"Common Table Expressions",description:"The WITH clause provides a way to write auxiliary statements for use in a larger query. These statements, which are often referred to as Common Table Expressions or CTEs, can be thought of as defining temporary tables that exist just for one query.",source:"@site/versioned_docs/version-2.x/operate-with-data/sql-queries/cte-queries.md",sourceDirName:"operate-with-data/sql-queries",slug:"/operate-with-data/sql-queries/cte-queries",permalink:"/docs/operate-with-data/sql-queries/cte-queries",draft:!1,unlisted:!1,editUrl:"https://github.com/apache/cloudberry-site/edit/main/versioned_docs/version-2.x/operate-with-data/sql-queries/cte-queries.md",tags:[],version:"2.x",lastUpdatedBy:"TomShawn",lastUpdatedAt:1749026520,formattedLastUpdatedAt:"Jun 4, 2025",frontMatter:{title:"Common Table Expressions"},sidebar:"docsbars",previous:{title:"Subqueries",permalink:"/docs/operate-with-data/sql-queries/subqueries"},next:{title:"Table Functions",permalink:"/docs/operate-with-data/sql-queries/table-functions"}},c={},l=[{value:"SELECT in a WITH clause",id:"select-in-a-with-clause",level:2},{value:"Data-modifying statements in a WITH clause",id:"data-modifying-statements-in-a-with-clause",level:2},{value:"Considerations",id:"considerations",level:2}];function d(e){const n={admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.a)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.h1,{id:"with-queries-common-table-expressions",children:"WITH Queries (Common Table Expressions)"}),"\n",(0,s.jsxs)(n.p,{children:["The ",(0,s.jsx)(n.code,{children:"WITH"})," clause provides a way to write auxiliary statements for use in a larger query. These statements, which are often referred to as Common Table Expressions or CTEs, can be thought of as defining temporary tables that exist just for one query."]}),"\n",(0,s.jsxs)(n.admonition,{type:"info",children:[(0,s.jsxs)(n.p,{children:["Limitations when using a ",(0,s.jsx)(n.code,{children:"WITH"})," clause in Apache Cloudberry include:"]}),(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["For a ",(0,s.jsx)(n.code,{children:"SELECT"})," command that includes a ",(0,s.jsx)(n.code,{children:"WITH"})," clause, the clause can contain at most a single clause that modifies table data (",(0,s.jsx)(n.code,{children:"INSERT"}),", ",(0,s.jsx)(n.code,{children:"UPDATE"}),", or ",(0,s.jsx)(n.code,{children:"DELETE"})," command)."]}),"\n",(0,s.jsxs)(n.li,{children:["For a data-modifying command (",(0,s.jsx)(n.code,{children:"INSERT"}),", ",(0,s.jsx)(n.code,{children:"UPDATE"}),", or ",(0,s.jsx)(n.code,{children:"DELETE"}),") that includes a ",(0,s.jsx)(n.code,{children:"WITH"})," clause, the clause can only contain a ",(0,s.jsx)(n.code,{children:"SELECT"})," command; it cannot contain a data-modifying command."]}),"\n"]})]}),"\n",(0,s.jsxs)(n.p,{children:["By default, Apache Cloudberry enables the ",(0,s.jsx)(n.code,{children:"RECURSIVE"})," keyword for the ",(0,s.jsx)(n.code,{children:"WITH"})," clause. ",(0,s.jsx)(n.code,{children:"RECURSIVE"})," can be deactivated by setting the server configuration parameter ",(0,s.jsx)(n.code,{children:"gp_recursive_cte"})," to ",(0,s.jsx)(n.code,{children:"false"}),"."]}),"\n",(0,s.jsx)(n.h2,{id:"select-in-a-with-clause",children:"SELECT in a WITH clause"}),"\n",(0,s.jsxs)(n.p,{children:["One use of CTEs is to break down complicated queries into simpler parts. The examples in this section show the ",(0,s.jsx)(n.code,{children:"WITH"})," clause being used with a ",(0,s.jsx)(n.code,{children:"SELECT"})," command. The example ",(0,s.jsx)(n.code,{children:"WITH"})," clauses can be used the same manner with ",(0,s.jsx)(n.code,{children:"INSERT"}),", ",(0,s.jsx)(n.code,{children:"UPDATE"}),", or ",(0,s.jsx)(n.code,{children:"DELETE"}),"."]}),"\n",(0,s.jsxs)(n.p,{children:["A ",(0,s.jsx)(n.code,{children:"SELECT"})," command in the ",(0,s.jsx)(n.code,{children:"WITH"})," clause is evaluated only once per execution of the parent query, even if it is referred to more than once by the parent query or sibling ",(0,s.jsx)(n.code,{children:"WITH"})," clauses. Thus, expensive calculations that are needed in multiple places can be placed within a ",(0,s.jsx)(n.code,{children:"WITH"})," clause to avoid redundant work. Another possible application is to prevent unwanted multiple evaluations of functions with side-effects. However, the other side of this coin is that the optimizer is less able to push restrictions from the parent query down into a ",(0,s.jsx)(n.code,{children:"WITH"})," query than an ordinary sub-query. The ",(0,s.jsx)(n.code,{children:"WITH"})," query will generally be evaluated as written, without suppression of rows that the parent query might discard afterwards. However, evaluation might stop early if the references to the query demand only a limited number of rows."]}),"\n",(0,s.jsx)(n.p,{children:"The following example query displays per-product sales totals in only the top sales regions:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH regional_sales AS (\n     SELECT region, SUM(amount) AS total_sales\n     FROM orders\n     GROUP BY region\n  ), top_regions AS (\n     SELECT region\n     FROM regional_sales\n     WHERE total_sales > (SELECT SUM(total_sales)/10 FROM regional_sales)\n  )\nSELECT region,\n    product,\n    SUM(quantity) AS product_units,\n    SUM(amount) AS product_sales\nFROM orders\nWHERE region IN (SELECT region FROM top_regions)\nGROUP BY region, product;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The ",(0,s.jsx)(n.code,{children:"WITH"})," clause defines two auxiliary statements named ",(0,s.jsx)(n.code,{children:"regional_sales"})," and ",(0,s.jsx)(n.code,{children:"top_regions"}),", where the output of ",(0,s.jsx)(n.code,{children:"regional_sales"})," is used in ",(0,s.jsx)(n.code,{children:"top_regions"})," and the output of ",(0,s.jsx)(n.code,{children:"top_regions"})," is used in the primary ",(0,s.jsx)(n.code,{children:"SELECT"})," query. The query could have been written without the ",(0,s.jsx)(n.code,{children:"WITH"})," clause, but would have required two levels of nested sub-",(0,s.jsx)(n.code,{children:"SELECT"}),"s."]}),"\n",(0,s.jsxs)(n.p,{children:["When you specify the optional ",(0,s.jsx)(n.code,{children:"RECURSIVE"})," keyword, the ",(0,s.jsx)(n.code,{children:"WITH"})," clause can accomplish operations not otherwise possible in standard SQL. Using ",(0,s.jsx)(n.code,{children:"RECURSIVE"}),", a ",(0,s.jsx)(n.code,{children:"WITH"})," query can refer to its own output. This simple example computes the sum of integers from 1 through 100:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE t(n) AS (\n    VALUES (1)\n  UNION ALL\n    SELECT n+1 FROM t WHERE n < 100\n)\nSELECT sum(n) FROM t;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The general form of a recursive ",(0,s.jsx)(n.code,{children:"WITH"})," query always follows the pattern of: a ",(0,s.jsx)(n.em,{children:"non-recursive term"}),", followed by a ",(0,s.jsx)(n.code,{children:"UNION"})," (or ",(0,s.jsx)(n.code,{children:"UNION ALL"}),"), and then a ",(0,s.jsx)(n.em,{children:"recursive term"}),", where only the ",(0,s.jsx)(n.em,{children:"recursive term"})," can contain a reference to the query output."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"<non_recursive_term> UNION [ ALL ] <recursive_term>\n"})}),"\n",(0,s.jsxs)(n.p,{children:["A recursive ",(0,s.jsx)(n.code,{children:"WITH"})," query that contains a ",(0,s.jsx)(n.code,{children:"UNION [ ALL ]"})," is evaluated as follows:"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["\n",(0,s.jsxs)(n.p,{children:["Evaluate the non-recursive term. For ",(0,s.jsx)(n.code,{children:"UNION"})," (but not ",(0,s.jsx)(n.code,{children:"UNION ALL"}),"), discard duplicate rows. Include all remaining rows in the result of the recursive query, and also place them in a temporary ",(0,s.jsx)(n.em,{children:"working table"}),"."]}),"\n"]}),"\n",(0,s.jsxs)(n.li,{children:["\n",(0,s.jsx)(n.p,{children:"As long as the working table is not empty, repeat these steps:"}),"\n",(0,s.jsxs)(n.ol,{children:["\n",(0,s.jsxs)(n.li,{children:["Evaluate the recursive term, substituting the current contents of the working table for the recursive self-reference. For ",(0,s.jsx)(n.code,{children:"UNION"})," (but not ",(0,s.jsx)(n.code,{children:"UNION ALL"}),"), discard duplicate rows and rows that duplicate any previous result row. Include all remaining rows in the result of the recursive query, and also place them in a temporary ",(0,s.jsx)(n.em,{children:"intermediate table"}),"."]}),"\n",(0,s.jsxs)(n.li,{children:["Replace the contents of the ",(0,s.jsx)(n.em,{children:"working table"})," with the contents of the ",(0,s.jsx)(n.em,{children:"intermediate table"}),", then empty the ",(0,s.jsx)(n.em,{children:"intermediate table"}),"."]}),"\n"]}),"\n",(0,s.jsx)(n.admonition,{type:"note",children:(0,s.jsxs)(n.p,{children:["While ",(0,s.jsx)(n.code,{children:"RECURSIVE"})," allows queries to be specified recursively, Apache Cloudberry evaluates such queries iteratively internally."]})}),"\n"]}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["In the example above, the working table has just a single row in each step, and it takes on the values from 1 through 100 in successive steps. In the 100th step, there is no output because of the ",(0,s.jsx)(n.code,{children:"WHERE"})," clause, and so the query terminates."]}),"\n",(0,s.jsxs)(n.p,{children:["Recursive ",(0,s.jsx)(n.code,{children:"WITH"})," queries are typically used to deal with hierarchical or tree-structured data. For example, this query locates all of the direct and indirect sub-parts of a product, given only a table that shows immediate inclusions:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE included_parts(sub_part, part, quantity) AS (\n    SELECT sub_part, part, quantity FROM parts WHERE part = 'our_product'\n  UNION ALL\n    SELECT p.sub_part, p.part, p.quantity * pr.quantity\n    FROM included_parts pr, parts p\n    WHERE p.part = pr.sub_part\n)\nSELECT sub_part, SUM(quantity) as total_quantity\nFROM included_parts\nGROUP BY sub_part;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["When working with recursive ",(0,s.jsx)(n.code,{children:"WITH"})," queries, you must ensure that the recursive part of the query eventually returns no tuples, or else the query loops indefinitely."]}),"\n",(0,s.jsxs)(n.p,{children:["For some queries, using ",(0,s.jsx)(n.code,{children:"UNION"})," instead of ",(0,s.jsx)(n.code,{children:"UNION ALL"})," can ensure that the recursive part of the query eventually returns no tuples by discarding rows that duplicate previous output rows. However, often a cycle does not involve output rows that are complete duplicates: it might be sufficient to check just one or a few fields to see if the same point has been reached before. The standard method for handling such situations is to compute an array of the visited values. For example, consider the following query that searches a table ",(0,s.jsx)(n.code,{children:"graph"})," using a ",(0,s.jsx)(n.code,{children:"link"})," field:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE search_graph(id, link, data, depth) AS (\n    SELECT g.id, g.link, g.data, 1\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["This query loops if the ",(0,s.jsx)(n.code,{children:"link"})," relationships contain cycles. Because the query requires a ",(0,s.jsx)(n.code,{children:"depth"})," output, changing ",(0,s.jsx)(n.code,{children:"UNION ALL"})," to ",(0,s.jsx)(n.code,{children:"UNION"})," does not eliminate the looping. Instead the query needs to recognize whether it has reached the same row again while following a particular path of links. This modified query adds two columns, ",(0,s.jsx)(n.code,{children:"path"})," and ",(0,s.jsx)(n.code,{children:"cycle"}),", to the loop-prone query:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE search_graph(id, link, data, depth, path, cycle) AS (\n    SELECT g.id, g.link, g.data, 1,\n      ARRAY[g.id],\n      false\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1,\n      path || g.id,\n      g.id = ANY(path)\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link AND NOT cycle\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,s.jsx)(n.p,{children:'Aside from detecting cycles, the array value is useful in its own right because it represents the "path" taken to reach any particular row.'}),"\n",(0,s.jsxs)(n.p,{children:["In the general case where more than one field needs to be checked to recognize a cycle, use an array of rows. For example, if we needed to compare fields ",(0,s.jsx)(n.code,{children:"f1"})," and ",(0,s.jsx)(n.code,{children:"f2"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE search_graph(id, link, data, depth, path, cycle) AS (\n    SELECT g.id, g.link, g.data, 1,\n      ARRAY[ROW(g.f1, g.f2)],\n      false\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1,\n      path || ROW(g.f1, g.f2),\n      ROW(g.f1, g.f2) = ANY(path)\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link AND NOT cycle\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"Tip:"})," Omit the ",(0,s.jsx)(n.code,{children:"ROW()"})," syntax in the case where only one field must be checked to recognize a cycle. This uses a simple array rather than a composite-type array, gaining efficiency."]}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.strong,{children:"Tip:"})," The recursive query evaluation algorithm produces its output in breadth-first search order. You can display the results in depth-first search order by making the outer query ",(0,s.jsx)(n.code,{children:"ORDER BY"}),' a "path" column constructed in this way.']}),"\n",(0,s.jsxs)(n.p,{children:["A helpful technique for testing a query when you are not certain if it might loop indefinitely is to place a ",(0,s.jsx)(n.code,{children:"LIMIT"})," in the parent query. For example, the following query would loop forever without the ",(0,s.jsx)(n.code,{children:"LIMIT"})," clause:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE t(n) AS (\n    SELECT 1\n  UNION ALL\n    SELECT n+1 FROM t\n)\nSELECT n FROM t LIMIT 100;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["This technique works because Apache Cloudberry evaluates only as many rows of a ",(0,s.jsx)(n.code,{children:"WITH"})," query as are actually fetched by the parent query. ",(0,s.jsx)(n.em,{children:"Using this technique in production environments is not recommended"}),", because other systems might work differently. Also, the technique might not work if the outer query sorts the recursive ",(0,s.jsx)(n.code,{children:"WITH"})," results or joins the results to another table, because in such cases the outer query will usually try to fetch all of the ",(0,s.jsx)(n.code,{children:"WITH"})," query's output anyway."]}),"\n",(0,s.jsxs)(n.p,{children:["A useful property of ",(0,s.jsx)(n.code,{children:"WITH"})," queries is that they are normally evaluated only once per execution of the parent query, even if they are referred to more than once by the parent query or sibling ",(0,s.jsx)(n.code,{children:"WITH"})," queries. Thus, expensive calculations that are needed in multiple places can be placed within a ",(0,s.jsx)(n.code,{children:"WITH"})," query to avoid redundant work. Another possible application is to prevent unwanted multiple evaluations of functions with side-effects. However, the other side of this coin is that the optimizer is not able to push restrictions from the parent query down into a multiply-referenced ",(0,s.jsx)(n.code,{children:"WITH"})," query, because that might affect all uses of the ",(0,s.jsx)(n.code,{children:"WITH"})," query's output when it should affect only one. Apache Cloudberry evalues the multiply-referenced ",(0,s.jsx)(n.code,{children:"WITH"})," query as written, without suppression of rows that the parent query might discard afterwards. (But, as mentioned above, evaluation might stop early if the reference(s) to the query demand only a limited number of rows.)"]}),"\n",(0,s.jsxs)(n.p,{children:["If a ",(0,s.jsx)(n.code,{children:"WITH"})," query is non-recursive and side-effect-free (that is, it is a ",(0,s.jsx)(n.code,{children:"SELECT"})," containing no volatile functions) then it can be folded into the parent query, allowing joint optimization of the two query levels. By default, this happens if the parent query references the ",(0,s.jsx)(n.code,{children:"WITH"})," query just once, but not if it references the ",(0,s.jsx)(n.code,{children:"WITH"})," query more than once. You can override that decision by specifying ",(0,s.jsx)(n.code,{children:"MATERIALIZED"})," to force separate calculation of the ",(0,s.jsx)(n.code,{children:"WITH"})," query, or by specifying ",(0,s.jsx)(n.code,{children:"NOT MATERIALIZED"})," to force it to be merged into the parent query. The latter choice risks duplicate computation of the ",(0,s.jsx)(n.code,{children:"WITH"})," query, but it can still give a net savings if each usage of the ",(0,s.jsx)(n.code,{children:"WITH"})," query needs only a small part of the ",(0,s.jsx)(n.code,{children:"WITH"})," query's full output."]}),"\n",(0,s.jsx)(n.p,{children:"A simple example of these rules follows:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH w AS (\n    SELECT * FROM big_table\n)\nSELECT * FROM w WHERE key = 123;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["This ",(0,s.jsx)(n.code,{children:"WITH"})," query will be folded, producing the same execution plan as:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"SELECT * FROM big_table WHERE key = 123;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["In particular, if there's an index on ",(0,s.jsx)(n.code,{children:"key"}),", Apache Cloudberry uses it to fetch just the rows having ",(0,s.jsx)(n.code,{children:"key = 123"}),". On the other hand, in:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH w AS (\n    SELECT * FROM big_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.key = w2.ref\nWHERE w2.key = 123;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["the ",(0,s.jsx)(n.code,{children:"WITH"})," query will be materialized, producing a temporary copy of ",(0,s.jsx)(n.code,{children:"big_table"})," that is then joined with itself \u2014 without benefit of any index. This query will run much more efficiently if written as:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH w AS NOT MATERIALIZED (\n    SELECT * FROM big_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.key = w2.ref\nWHERE w2.key = 123;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["so that the parent query's restrictions can be applied directly to scans of ",(0,s.jsx)(n.code,{children:"big_table"}),"."]}),"\n",(0,s.jsxs)(n.p,{children:["An example where ",(0,s.jsx)(n.code,{children:"NOT MATERIALIZED"})," could be undesirable is:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH w AS (\n    SELECT key, very_expensive_function(val) as f FROM some_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.f = w2.f;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Here, materialization of the ",(0,s.jsx)(n.code,{children:"WITH"})," query ensures that Apache Cloudberry evaluations ",(0,s.jsx)(n.code,{children:"very_expensive_function"})," only once per table row, not twice."]}),"\n",(0,s.jsx)(n.h2,{id:"data-modifying-statements-in-a-with-clause",children:"Data-modifying statements in a WITH clause"}),"\n",(0,s.jsxs)(n.p,{children:["For a ",(0,s.jsx)(n.code,{children:"SELECT"})," command, you can use the data-modifying commands ",(0,s.jsx)(n.code,{children:"INSERT"}),", ",(0,s.jsx)(n.code,{children:"UPDATE"}),", or ",(0,s.jsx)(n.code,{children:"DELETE"})," in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause. This allows you to perform several different operations in the same query."]}),"\n",(0,s.jsxs)(n.p,{children:["A data-modifying statement in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause is run exactly once, and always to completion, independently of whether the primary query reads all (or indeed any) of the output. This is different from the rule when using ",(0,s.jsx)(n.code,{children:"SELECT"})," in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause, the execution of a ",(0,s.jsx)(n.code,{children:"SELECT"})," continues only as long as the primary query demands its output."]}),"\n",(0,s.jsxs)(n.p,{children:["This simple CTE query deletes rows from ",(0,s.jsx)(n.code,{children:"products"}),". The ",(0,s.jsx)(n.code,{children:"DELETE"})," in the ",(0,s.jsx)(n.code,{children:"WITH"})," clause deletes the specified rows from products, returning their contents by means of its ",(0,s.jsx)(n.code,{children:"RETURNING"})," clause."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH deleted_rows AS (\n    DELETE FROM products\n    WHERE\n        \"date\" >= '2010-10-01' AND\n        \"date\" < '2010-11-01'\n    RETURNING *\n)\nSELECT * FROM deleted_rows;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Data-modifying statements in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause must have ",(0,s.jsx)(n.code,{children:"RETURNING"})," clauses, as shown in the previous example. It is the output of the ",(0,s.jsx)(n.code,{children:"RETURNING"})," clause, ",(0,s.jsx)(n.em,{children:"not"})," the target table of the data-modifying statement, that forms the temporary table that can be referred to by the rest of the query. If a data-modifying statement in a ",(0,s.jsx)(n.code,{children:"WITH"})," lacks a ",(0,s.jsx)(n.code,{children:"RETURNING"})," clause, then it forms no temporary table and cannot be referred to in the rest of the query. Apache Cloudberry runs such a statement nonetheless."]}),"\n",(0,s.jsxs)(n.p,{children:["If the optional ",(0,s.jsx)(n.code,{children:"RECURSIVE"})," keyword is enabled, recursive self-references in data-modifying statements are not allowed. In some cases it is possible to work around this limitation by referring to the output of a recursive ",(0,s.jsx)(n.code,{children:"WITH"}),". For example, this query would remove all direct and indirect subparts of a product:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH RECURSIVE included_parts(sub_part, part) AS (\n    SELECT sub_part, part FROM parts WHERE part = 'our_product'\n  UNION ALL\n    SELECT p.sub_part, p.part\n    FROM included_parts pr, parts p\n    WHERE p.part = pr.sub_part\n  )\nDELETE FROM parts\n  WHERE part IN (SELECT part FROM included_parts);\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The sub-statements in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause are run concurrently with each other and with the main query. Therefore, when using a data-modifying statement in a ",(0,s.jsx)(n.code,{children:"WITH"}),", the order in which the specified updates actually happen is unpredictable. All of the statements are run wth the same ",(0,s.jsx)(n.em,{children:"snapshot"}),". The effects of the statement are not visible on the target tables. This alleviates the effects of the unpredictability of the actual order of row updates, and means thats the ",(0,s.jsx)(n.code,{children:"RETURNING"})," data is the only way to communicate changes between different ",(0,s.jsx)(n.code,{children:"WITH"})," sub-statements and the main query."]}),"\n",(0,s.jsxs)(n.p,{children:["In this example, the outer ",(0,s.jsx)(n.code,{children:"SELECT"})," returns the original prices before the action of the ",(0,s.jsx)(n.code,{children:"UPDATE"})," in the ",(0,s.jsx)(n.code,{children:"WITH"})," clause:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH t AS (\n    UPDATE products SET price = price * 1.05\n    RETURNING *\n)\nSELECT * FROM products;\n"})}),"\n",(0,s.jsxs)(n.p,{children:["In this example, the outer ",(0,s.jsx)(n.code,{children:"SELECT"})," returns the updated data:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-sql",children:"WITH t AS (\n    UPDATE products SET price = price * 1.05\n    RETURNING *\n)\nSELECT * FROM t;\n"})}),"\n",(0,s.jsx)(n.p,{children:"Updating the same row twice in a single statement is not supported. The effects of such a statement will not be predictable. Only one of the modifications takes place, but it is not easy (and sometimes not possible) to predict which modification occurs."}),"\n",(0,s.jsxs)(n.p,{children:["Any table used as the target of a data-modifying statement in a ",(0,s.jsx)(n.code,{children:"WITH"})," clause must not have a conditional rule, nor an ",(0,s.jsx)(n.code,{children:"ALSO"})," rule, nor an ",(0,s.jsx)(n.code,{children:"INSTEAD"})," rule that expands to multiple statements."]}),"\n",(0,s.jsx)(n.h2,{id:"considerations",children:"Considerations"}),"\n",(0,s.jsxs)(n.p,{children:["When constructing ",(0,s.jsx)(n.code,{children:"WITH"})," queries, keep the following in mind:"]}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.code,{children:"SELECT FOR UPDATE"})," cannot be inlined."]}),"\n",(0,s.jsxs)(n.li,{children:["Apache Cloudberry inlines multiply-referenced CTEs only when requested (by specifying ",(0,s.jsx)(n.code,{children:"NOT MATERIALIZED"}),")."]}),"\n",(0,s.jsx)(n.li,{children:"Multiply-referenced CTEs cannot be inlined if they contain outer self-references."}),"\n",(0,s.jsx)(n.li,{children:"Apache Cloudberry does not inline when the CTE includes a volatile function."}),"\n",(0,s.jsxs)(n.li,{children:["An ",(0,s.jsx)(n.code,{children:"ORDER BY"})," in the subquery or CTE does not force an ordering for the whole query."]}),"\n",(0,s.jsxs)(n.li,{children:["Apache Cloudberry always materializes a CTE term in a query. Due to these reasons:","\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsx)(n.li,{children:"A query that should touch a small amount of data might instead read a whole table, and possibly spill to a temporary file."}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.code,{children:"UPDATE"})," or ",(0,s.jsx)(n.code,{children:"DELETE FROM"})," statements are not permitted in a CTE term, as it acts more like a read-only temporary table than a dynamic view."]}),"\n"]}),"\n"]}),"\n",(0,s.jsx)(n.li,{children:"While inlining is generally a huge win, there are certain boundary cases where it is not; for example, when a non-trivial expression will be inlined in multiple places."}),"\n",(0,s.jsxs)(n.li,{children:["The GPORCA query optimizer does not support ",(0,s.jsx)(n.code,{children:"[NOT] MATERIALIZED"}),"."]}),"\n"]})]})}function h(e={}){const{wrapper:n}={...(0,r.a)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(d,{...e})}):d(e)}},11151:(e,n,t)=>{t.d(n,{Z:()=>o,a:()=>a});var s=t(67294);const r={},i=s.createContext(r);function a(e){const n=s.useContext(i);return s.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:a(e.components),s.createElement(i.Provider,{value:n},e.children)}}}]);